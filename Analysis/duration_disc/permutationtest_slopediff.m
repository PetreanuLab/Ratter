function [s p] = permutationtest_slopediff(x1,y1, x2,y2)
% takes two pairs of data, (x1,y1) and (x2,y2),
% does a permutation test by shuffling the tuples in each, 
% and tests whether the slopes are significantly different.

